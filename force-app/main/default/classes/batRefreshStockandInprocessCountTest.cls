@isTest
private class batRefreshStockandInprocessCountTest
{
    static testmethod void constructorTest()
    {
        Test.startTest();
        batRefreshStockandInprocessCount.testMeth();
        Test.stopTest();
    }
}